{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$TOOL_INPUT_FILE_PATH\" == *.rs ]] || [[ \"$TOOL_INPUT_file_path\" == *.rs ]]; then cargo fmt --quiet 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
