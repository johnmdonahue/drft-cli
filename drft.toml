# drft.toml — drft dogfooding itself

[parsers]
markdown = true

[parsers.rust]
glob = "*.rs"
command = "./scripts/parse-rust-docs.sh"
types = ["comment"]

[rules]
broken-link = "warn"
cycle = "warn"
stale = "warn"
containment = "warn"
encapsulation = "warn"
